//assignment 13
var favTransport = ["Hilux", "ferrari", "scooty",];
for (var i = 0; i < favTransport.length; i++) {
    var transport = favTransport[i];
    console.log("I would like to own a ".concat(transport, "."));
}
